A simple github email scraper

This gets all the repositories of a user and then goes through all do commits, looking for a time where the user may have accidentally committed using their real email


Installation

git clone https://github.com/archonxk/Github-Commits-Scraper.git OR download the zip and unzip
pip install -r requirements.txt
python main.py

